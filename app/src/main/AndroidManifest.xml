<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.complaint_app">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.admin.complaint_app.view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.signup.signup" />
        <activity android:name=".view.profile.profile" />
        <activity android:name=".view.login.login" />
        <activity android:name=".view.filecomplaint.filecomplaint" />
        <activity android:name=".view.viewcomplaint.viewcomplaint" />
        <activity android:name=".view.viewstatus.viewstatus"></activity>
    </application>

</manifest>